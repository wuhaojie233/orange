import React, { FC } from 'react'
import { Redirect } from 'react-router-dom'

const AuthLogin = (viewComponent: FC) => {
    return viewComponent
//   return () => {
//     return <Redirect to="/login"></Redirect>
//   }
}

export default AuthLogin
